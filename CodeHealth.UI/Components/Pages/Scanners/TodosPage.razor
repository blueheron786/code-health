@page "/project/{ProjectId}/todos"

<GenericScanner 
    ProjectId="@ProjectId"
    ScannerName="TODO Comments"
    ScannerData="@ScannerData"
    ProjectRootDirectory="@ProjectRootDirectory"
    GoBackPage="todos"
    ValueFormatter="@(r => r.Message)"
    BadgeClass="@GetTodoBadgeClass"
    BadgeText="@GetTodoBadgeText" />
