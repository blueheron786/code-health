@page "/project/{ProjectId}/cyclomatic-complexity"
@inherits CyclomaticComplexityPage

<GenericScanner 
    ProjectId="@ProjectId"
    ScannerName="Cyclomatic Complexity"
    ScannerData="@ScannerData"
    ProjectRootDirectory="@ProjectRootDirectory"
    ValueFormatter="@(r => r.Metric.Value.ToString())"
    BadgeClass="@GetComplexityClass"
    BadgeText="@GetComplexityText" />