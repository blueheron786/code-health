@page "/projects"
@inherits ProjectsPage

<h1>Projects</h1>

@if (projects == null || projects.Count == 0)
{
    <p>No projects found.</p>
}
else
{
    <ul>
        @foreach (var project in projects)
        {
            <li>
                <strong>@project.Name</strong> - Last run: @project.LastRunTime
            </li>
        }
    </ul>
}